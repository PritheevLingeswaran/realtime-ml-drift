# Backend origin for a standalone deployment of this dashboard (e.g. on Vercel).
# Leave unset for local `vite dev` (proxied to :8000) or when FastAPI serves
# this bundle same-origin. No trailing slash.
VITE_API_BASE_URL=https://your-backend.onrender.com
