# Data

- `raw/streams/`: JSONL streams for deterministic replay.
- `processed/features/`: optional feature dumps (debug).
- `processed/labels/`: optional labels (future extension).
- `processed/snapshots/`: snapshots of drift state / threshold state for replay/debug.
